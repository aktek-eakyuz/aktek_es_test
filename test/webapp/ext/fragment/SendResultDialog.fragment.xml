<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog
        id="sendResultDialog"
        title="{i18n>sendResultDialog.title}"
        contentWidth="40rem"
        resizable="true">
        <Table
            id="resultTable"
            items="{results>/messages}">
            <columns>
                <Column width="3rem">
                    <Label text="" />
                </Column>
                <Column width="12rem">
                    <Label text="{i18n>sendResultDialog.columnId}" />
                </Column>
                <Column>
                    <Label text="{i18n>sendResultDialog.columnMessage}" />
                </Column>
            </columns>
            <items>
                <ColumnListItem>
                    <cells>
                        <core:Icon
                            src="{= ${results>severity} >= 4 ? 'sap-icon://error' : 'sap-icon://sys-enter-2'}"
                            color="{= ${results>severity} >= 4 ? '#bb0000' : '#2b7c2b'}" />
                        <Text text="{results>id}" />
                        <Text text="{results>message}" />
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
        <endButton>
            <Button text="{i18n>sendResultDialog.closeButton}" press=".onCloseResultDialog" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>